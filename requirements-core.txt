# Core Dependencies (Cross-Platform)
# ====================================
# These work on both Mac and PC

# NumPy - using 1.24.3 for compatibility with both old and new packages
# Compatible with: TensorFlow 2.15.0, PyTorch 2.1.0, OpenCV 4.8.x, MediaPipe
numpy==1.24.3

# Computer Vision
opencv-python-headless==4.8.1.78
mediapipe==0.10.8

# Image Processing
pillow==10.1.0

# Audio (for speech recognition)
pyaudio==0.2.14

# HTTP Requests (for Rodin API and ComfyUI client)
requests==2.31.0

# Utilities
tqdm==4.66.1

# WebSocket UI (for browser-based Three.js interface)
websockets>=12.0
aiohttp>=3.9.0

# Firebase Cloud Storage (optional - for gallery feature)
firebase-admin>=6.2.0

# NOTE: The following packages from the original repo are NOT needed for this minimal pipeline:
# - chumpy: Only used for cage deformation (removed)
# - trimesh, pyglet, networkx: Only used for 3D mesh processing (removed)
# - scipy: Only used for cage deformation math (removed)
